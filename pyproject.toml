[project]
name = "micro-mdt"
version = "0.1.0"
description = "A low-cost Micro-MDT multi-agent medical safety workflow demo."
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Micro-MDT Contributors" }]
dependencies = []

[project.scripts]
micro-mdt = "micro_mdt.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

